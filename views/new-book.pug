extends layout

block content
  h1 New Book
  if errors
    h2.error Oooops!
    ul.error
      each error in errors
        li= error.message
  +bookForm("/books/new", "Create New Book", book)
  p
    a.button(href='/') Cancel